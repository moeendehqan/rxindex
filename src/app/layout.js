
export const metadata = {
  title: 'رند ترید',
  description: 'دستیار معاملاتی',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
